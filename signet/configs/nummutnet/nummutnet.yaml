config:
    # IDs & paths
    data_id: "datasets/detector"
    model_name: "nummutnet"

    # Training params
    iterations: 2000
    batch_size: 50
    lr: 0.0001

    # WANDB params
    enable_logging: True
    wandb_project_id: "nummutnet"

    # Network params
    num_hidden_layers: 3
    num_neurons: 300

    # Misc
    device: "cpu"