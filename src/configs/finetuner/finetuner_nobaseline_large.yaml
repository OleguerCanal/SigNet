config:
    # IDs & paths
    data_id: "exp_not_norm/sd1.5"
    model_id: "finetuner_not_norm_no_baseline_large_0_15"
    models_dir: "../trained_models/exp_not_norm"

    # Training params
    source: "generator_large"
    network_type: "low"
    iterations: 70
    num_classes: 72
    sigmoid_params: [[1000, 1000], [10000, 2000], [100000, 5000]]
    batch_size: 283
    lr: 0.0015

    # WANDB params
    enable_logging: True
    wandb_project_id: "finetuner_no_baseline"

    # Network params
    num_hidden_layers: 3
    num_neurons: 100

    # Misc
    device: "cuda"