config:
    # IDs & paths
    data_id: "exp_real_data"
    model_id: "finetuner_real_oversampled_low"
    models_dir: "../trained_models/exp_real_data"
    load_data: False
    
    # Training params
    source: "real_low"
    network_type: "low"
    iterations: 150
    num_classes: 72
    sigmoid_params: [[500, 150], [5000, 1000], [10000, 5000]]
    batch_size: 283
    lr: 0.0015

    # WANDB params
    enable_logging: True
    wandb_project_id: "finetuner_real_data"

    # Network params
    num_hidden_layers: 1
    num_neurons: 100

    # Misc
    device: "cuda"