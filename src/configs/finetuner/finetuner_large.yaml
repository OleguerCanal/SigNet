config:
    # IDs & paths
    data_id: "exp_cleaned_generator"
    model_id: "finetuner_cleaned_generator_large"
    models_dir: "../trained_models/exp_cleaned_generator"

    # Training params
    source: "generator_large"
    network_type: "large"
    iterations: 100
    num_classes: 72
    sigmoid_params: [50000, 10000]
    batch_size: 500
    lr: 0.0005

    # WANDB params
    enable_logging: True
    wandb_project_id: "finetuner_generator_clean"

    # Network params
    num_hidden_layers: 2
    num_neurons: 300

    # Misc
    device: "cuda"