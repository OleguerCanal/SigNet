config:
    # IDs & paths
    data_id: "datasets/detector"
    model_id: "classifier_final"
    models_dir: "../trained_models/test_exp/"

    # Training params
    iterations: 20
    num_classes: 72
    sigmoid_params: [5000,1000]
    batch_size: 100
    lr: 0.00043

    # WANDB params
    enable_logging: True
    wandb_project_id: "classifier"

    # Network params
    num_hidden_layers: 3
    num_neurons: 260

    # Misc
    device: "cuda"